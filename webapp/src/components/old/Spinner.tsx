import React, { Fragment } from 'react';

import {
  Image,
} from '@chakra-ui/react'

export default () => {
	return (
		<Fragment>
			<Image src='/spinner.gif' alt='loading...' />
		</Fragment>
	);
};
